```bash
# check the <main> as the current branch
git checkout main  

# pull from remote <main> to local 
git pull origin main    

# local: create new branch base on the current branch
git branch {new-branch-name}

# checkout 
git checkout {branch-name}

# local: delete
git branch -d {branch-name}
```
